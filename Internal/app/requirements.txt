# Runtime lock for the consumer-visible feature set. Update deliberately and run
# the clean-environment import gate before changing versions.
faster-whisper==1.2.1
sounddevice==0.5.5
numpy==2.2.6
keyboard==0.13.5
mouse==0.7.1
pyperclip==1.11.0
pystray==0.19.5
Pillow==12.3.0
pywebview==6.2.1

# Windows correction observation and DirectML transcription support.
# Mumble Search itself uses only the Python standard library.
comtypes==1.4.16
pywin32==312
# sherpa-onnx powers the optional SenseVoice + DirectML transcription path.
sherpa-onnx==1.13.4

# Meeting import/resampling.
soundfile==0.14.0
scipy==1.18.0

# Reader formats advertised by the file picker.
PyMuPDF==1.27.2.3
python-docx==1.2.0
beautifulsoup4==4.15.0
EbookLib==0.20
openpyxl==3.1.5
python-pptx==1.0.2
odfpy==1.4.1

# Optional services are intentionally not installed by default:
# - Account/Sync stays hidden until Mumble operates a supported backend.
# - Deep speaker diarisation is import-guarded and falls back to one speaker.
