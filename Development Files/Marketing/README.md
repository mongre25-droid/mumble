# Mumble marketing

This area contains current product communication material:

- `Website/` is the product website and its build checks.
- `Social Campaigns/` contains campaign source files, generated artwork, and contact sheets.

Marketing claims must match the current application in `Internal/app/` and the current product record in `Core/README.html`.
